# ── Database ──────────────────────────────────────────────────────────────────
DB_USER=craftalism
DB_PASSWORD=change_me_in_production
RCON_PASSWORD=change_me_in_production

# ── Auth Server ───────────────────────────────────────────────────────────────
# Public URL of this auth server (used as JWT issuer claim)
AUTH_ISSUER_URI=https://auth.example.com
AUTH_TOKEN_PATH=http://craftalism-auth-server:9000/oauth2/token
DASHBOARD_BFF_TOKEN_URL=http://auth-server:9000/oauth2/token
DASHBOARD_BFF_CLIENT_ID=dashboard-bff
# Generate a strong secret: openssl rand -base64 32
DASHBOARD_BFF_CLIENT_SECRET=change_me_in_production

# ── Host Port Bindings ────────────────────────────────────────────────────────
# Production behind craftalism-infra should keep these on loopback only.
AUTH_SERVER_HOST=127.0.0.1
AUTH_SERVER_PORT=9000
API_HOST=127.0.0.1
API_PORT=3000
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=8080

# ── Runtime Profile ───────────────────────────────────────────────────────────
# Controls deployment-owned memory defaults for compose-managed services.
# Supported values: small-host, standard
CRAFTALISM_RUNTIME_PROFILE=small-host
# The small-host defaults below are supplied by docker-compose.yml and
# scripts/runtime-profile.sh. Uncomment only when intentionally overriding the
# selected profile for this host.
# API_JAVA_TOOL_OPTIONS=-Xms48m -Xmx144m -Xss512k -XX:+UseSerialGC -XX:MaxMetaspaceSize=128m -XX:ReservedCodeCacheSize=48m -XX:+ExitOnOutOfMemoryError
# API_MEM_LIMIT=576m
# API_MEM_RESERVATION=192m
# API_SPRING_JPA_SHOW_SQL=false
# API_SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=false
# API_SPRINGDOC_API_DOCS_ENABLED=false
# API_SPRINGDOC_SWAGGER_UI_ENABLED=false
# API_SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=3
# API_SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=1
#
# AUTH_SERVER_JAVA_TOOL_OPTIONS=-Xms48m -Xmx112m -Xss512k -XX:+UseSerialGC -XX:MaxMetaspaceSize=72m -XX:ReservedCodeCacheSize=32m -XX:+ExitOnOutOfMemoryError
# AUTH_SERVER_MEM_LIMIT=320m
# AUTH_SERVER_MEM_RESERVATION=128m
# AUTH_SERVER_SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=2
# AUTH_SERVER_SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=1
#
# POSTGRES_SHARED_BUFFERS=64MB
# POSTGRES_WORK_MEM=2MB
# POSTGRES_MAINTENANCE_WORK_MEM=32MB
# POSTGRES_MEM_LIMIT=192m
# POSTGRES_MEM_RESERVATION=96m
#
# DASHBOARD_MEM_LIMIT=64m
# DASHBOARD_MEM_RESERVATION=16m
# DASHBOARD_BFF_MEM_LIMIT=96m
# DASHBOARD_BFF_MEM_RESERVATION=32m
# EDGE_MEM_LIMIT=96m
# EDGE_MEM_RESERVATION=32m

# ── Minecraft Server Client ───────────────────────────────────────────────────
MINECRAFT_CLIENT_ID=minecraft-server
# Generate a strong secret: openssl rand -base64 32
MINECRAFT_CLIENT_SECRET=change_me_in_production
# MINECRAFT_INIT_MEMORY=768M
# MINECRAFT_MEMORY=768M
# MINECRAFT_VIEW_DISTANCE=6
# MINECRAFT_SIMULATION_DISTANCE=4
# MINECRAFT_MEM_LIMIT=1280m
# MINECRAFT_MEM_RESERVATION=768m
# USE_AIKAR_FLAGS=false
# MINECRAFT_JVM_XX_OPTS=-XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+DisableExplicitGC
MARKET_API_BASE_URL=http://craftalism-api:8080
MARKET_API_SNAPSHOT_PATH=/api/market/snapshot
MARKET_API_QUOTE_PATH=/api/market/quotes
MARKET_API_EXECUTE_PATH=/api/market/execute
# MARKET_API_AUTH_TOKEN=eyJhbGciOi...
# MARKET_API_CONNECT_TIMEOUT_MS=3000
# MARKET_API_REQUEST_TIMEOUT_MS=5000
# MARKET_API_QUOTE_DEBOUNCE_TICKS=6

# ── API Market Runtime Guardrails ─────────────────────────────────────────────
# Deployment-owned request pressure guardrails passed through to craftalism-api.
# Set a max value to 0 only when intentionally disabling that API limiter.
MARKET_QUOTE_RATE_LIMIT_MAX_REQUESTS=120
MARKET_EXECUTE_RATE_LIMIT_MAX_REQUESTS=30
MARKET_RATE_LIMIT_WINDOW_SECONDS=60

# ── RSA Keys ─────────────────────────────────────────────────────────────────
# Paste as single-line values using literal \n separators.
RSA_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_BASE64_KEY_HERE\n-----END PRIVATE KEY-----
RSA_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nYOUR_BASE64_KEY_HERE\n-----END PUBLIC KEY-----

# ── Production release versions (must be immutable; never use latest) ───────
AUTH_SERVER_VERSION=1.0.0
AUTH_SERVER_DIGEST=sha256:replace_with_release_digest
API_VERSION=1.0.0
API_DIGEST=sha256:replace_with_release_digest
DASHBOARD_VERSION=1.0.0
DASHBOARD_DIGEST=sha256:replace_with_release_digest
ECONOMY_VERSION=1.5
MARKET_VERSION=1.0.0
POSTGRES_VERSION=18-alpine
POSTGRES_DIGEST=sha256:replace_with_release_digest
MINECRAFT_IMAGE_VERSION=java21
MINECRAFT_IMAGE_DIGEST=sha256:replace_with_release_digest
# Optional Paper/Bukkit/Spigot plugin jar URLs appended after Craftalism plugins.
# Do not put Forge/Fabric/NeoForge mods here; Paper cannot load them.
# MINECRAFT_EXTRA_PLUGIN_URLS=https://example.com/plugin-one.jar,https://example.com/plugin-two.jar

# ── Optional standalone edge profile ──────────────────────────────────────────
# Only set these if you intentionally run the local `edge` service profile
# instead of the infra-managed host Caddy proxy.
# CADDY_IMAGE=caddy:2.9-alpine@sha256:replace_with_release_digest
# DASHBOARD_SITE_ADDRESS=dash.example.com
# AUTH_SITE_ADDRESS=auth.example.com
# API_SITE_ADDRESS=api.example.com
# DASHBOARD_BASIC_AUTH_USERNAME=craftalism
# DASHBOARD_BASIC_AUTH_PASSWORD_HASH=$$2a$$14$$replace_with_bcrypt_hash

# ── Local development overrides (optional) ───────────────────────────────────
# AUTH_SERVER_BUILD_CONTEXT=../craftalism-authorization-server/java
# API_BUILD_CONTEXT=../craftalism-api/java
# DASHBOARD_BUILD_CONTEXT=../craftalism-dashboard/react
# ECONOMY_PLUGIN_JAR=/absolute/path/to/.local-dev/craftalism-economy.jar
# MARKET_PLUGIN_JAR=/absolute/path/to/.local-dev/craftalism-market.jar
# CLEAN_PLUGIN_BUILD=1
# AUTH_SERVER_DOCKERFILE=Dockerfile
# API_DOCKERFILE=Dockerfile
# LOCAL_API_SPRINGDOC_API_DOCS_ENABLED=true
# LOCAL_API_SPRINGDOC_SWAGGER_UI_ENABLED=true
# DASHBOARD_DOCKERFILE=Dockerfile

# ── Staging/test overrides (optional) ────────────────────────────────────────
# AUTH_SERVER_CI_TAG=main-a1b2c3d
# API_CI_TAG=main-a1b2c3d
# DASHBOARD_CI_TAG=main-a1b2c3d
# AUTH_SERVER_GIT_SHA=a1b2c3d
# API_GIT_SHA=a1b2c3d
# DASHBOARD_GIT_SHA=a1b2c3d
# ECONOMY_GIT_SHA=a1b2c3d
# MARKET_GIT_SHA=a1b2c3d
